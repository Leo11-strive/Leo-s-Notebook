# Polymorphism

## virtual

- virtual function: a member function in a base class that you expect to override in derived classes.

- the compiler checks whether the static type has the function, then check whether it is virtual, and then check whether the dynamic type has the function.

- the virtual function can be passed on to the derived class.

- only happens in reference and pointer types.

