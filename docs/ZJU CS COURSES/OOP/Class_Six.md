# Class Six

## Preclass Quiz

* right value reference cannot be initialized by left value

## New and Delete

* `new` and `delete` are used to allocate and deallocate memory dynamically
* `new <tpye/class> [size]` and `delete []`
* `new <class>`: run the constructor, and return the address of the object(this pointer)

## Inside Class

* c++ class can have two constructors

### Overloaded constructors



